services:
   glymage:
     image: glyomics/glymage:latest
     ports:
       - 10985:10985
     volumes:
       - ./glymage-local.ini:/code/glymage-local.ini
networks:
   default:
     name: ${GLYGEN_NETWORK:-glygen-network}
     external: true
